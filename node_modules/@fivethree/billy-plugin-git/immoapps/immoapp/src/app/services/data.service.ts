import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { environment } from '@environments/environment';

@Injectable({
  providedIn: 'root'
})
export class DataService {

  private users = environment.baseURL + '/api/users';
  private checklists = environment.baseURL + '/api/checklisttemplates';

  constructor(public http: HttpClient) {

  }

  // loadUsers(): Observable<User[]> {
  //   console.log('load users');
  //   const users = this.store.select('users');
  //   this.http.get(`${this.users}`)
  //     .pipe(
  //       tap(console.log),
  //     ).subscribe((uss) => {
  //       this.store.dispatch(new Action('UPDATE', uss));
  //     });

  //   return users;

  // }

  // loadChecklists(): Observable<ChecklistTemplate[]> {
  //   const checklists = this.store.select('checklistTemplates');
  //   console.log('no checklists in store');
  //   console.log('load checklists from server');
  //   this.http.get<LoadChecklistPayload>(`${this.checklists}`)
  //     .pipe(
  //       tap(console.log),
  //     ).subscribe((checks) => {
  //       console.log('###', 'dispatch', checks);
  //       this.store.dispatch(new Action('UPDATE', checks));
  //     });

  //   return checklists;

  // }




}
