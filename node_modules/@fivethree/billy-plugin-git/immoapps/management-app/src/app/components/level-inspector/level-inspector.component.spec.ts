import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LevelInspectorComponent } from './level-inspector.component';

describe('LevelInspectorComponent', () => {
  let component: LevelInspectorComponent;
  let fixture: ComponentFixture<LevelInspectorComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LevelInspectorComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LevelInspectorComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
