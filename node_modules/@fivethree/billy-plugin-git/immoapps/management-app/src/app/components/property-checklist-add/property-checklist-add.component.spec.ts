import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PropertyChecklistAddComponent } from './property-checklist-add.component';

describe('PropertyChecklistAddComponent', () => {
  let component: PropertyChecklistAddComponent;
  let fixture: ComponentFixture<PropertyChecklistAddComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PropertyChecklistAddComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PropertyChecklistAddComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
