import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LevelInspectorKriteriumComponent } from './level-inspector-kriterium.component';

describe('LevelInspectorKriteriumComponent', () => {
  let component: LevelInspectorKriteriumComponent;
  let fixture: ComponentFixture<LevelInspectorKriteriumComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LevelInspectorKriteriumComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LevelInspectorKriteriumComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
