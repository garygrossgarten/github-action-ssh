import { Component, OnInit, Output, EventEmitter } from '@angular/core';
import { NavParams } from '@ionic/angular';

@Component({
  selector: 'app-level-actions',
  templateUrl: './level-actions.component.html',
  styleUrls: ['./level-actions.component.scss']
})
export class LevelActionsComponent implements OnInit {

  constructor(private navParams: NavParams) { }

  ngOnInit() {
  }


  deleteLevel(){
    this.navParams.get('controller').dismiss({},'delete');
  }

}
