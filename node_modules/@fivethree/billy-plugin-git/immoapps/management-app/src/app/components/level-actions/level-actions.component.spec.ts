import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LevelActionsComponent } from './level-actions.component';

describe('LevelActionsComponent', () => {
  let component: LevelActionsComponent;
  let fixture: ComponentFixture<LevelActionsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LevelActionsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LevelActionsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
