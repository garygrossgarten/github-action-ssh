import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { HausverwalterPage } from './hausverwalter.page';

describe('HausverwalterPage', () => {
  let component: HausverwalterPage;
  let fixture: ComponentFixture<HausverwalterPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ HausverwalterPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(HausverwalterPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
