import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ChecklistCreatePage } from './checklist-create.page';

describe('ChecklistCreatePage', () => {
  let component: ChecklistCreatePage;
  let fixture: ComponentFixture<ChecklistCreatePage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ChecklistCreatePage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ChecklistCreatePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
