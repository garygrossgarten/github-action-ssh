import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { VerwalterPage } from './verwalter.page';

describe('VerwalterPage', () => {
  let component: VerwalterPage;
  let fixture: ComponentFixture<VerwalterPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ VerwalterPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(VerwalterPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
