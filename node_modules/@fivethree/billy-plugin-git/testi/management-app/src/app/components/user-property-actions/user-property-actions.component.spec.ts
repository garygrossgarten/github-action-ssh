import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { UserPropertyActionsComponent } from './user-property-actions.component';

describe('UserPropertyActionsComponent', () => {
  let component: UserPropertyActionsComponent;
  let fixture: ComponentFixture<UserPropertyActionsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ UserPropertyActionsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(UserPropertyActionsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
