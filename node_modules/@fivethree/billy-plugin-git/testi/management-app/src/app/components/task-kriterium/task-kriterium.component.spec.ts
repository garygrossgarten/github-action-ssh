import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TaskKriteriumComponent } from './task-kriterium.component';

describe('TaskKriteriumComponent', () => {
  let component: TaskKriteriumComponent;
  let fixture: ComponentFixture<TaskKriteriumComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TaskKriteriumComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TaskKriteriumComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
