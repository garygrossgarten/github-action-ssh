import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PropertyChecklistActionsComponent } from './property-checklist-actions.component';

describe('PropertyChecklistActionsComponent', () => {
  let component: PropertyChecklistActionsComponent;
  let fixture: ComponentFixture<PropertyChecklistActionsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PropertyChecklistActionsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PropertyChecklistActionsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
