import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { InspectionKriteriumComponent } from './inspection-kriterium.component';

describe('InspectionKriteriumComponent', () => {
  let component: InspectionKriteriumComponent;
  let fixture: ComponentFixture<InspectionKriteriumComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ InspectionKriteriumComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(InspectionKriteriumComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
