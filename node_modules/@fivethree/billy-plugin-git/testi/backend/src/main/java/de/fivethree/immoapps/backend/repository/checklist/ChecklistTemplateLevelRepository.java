package de.fivethree.immoapps.backend.repository.checklist;

import de.fivethree.immoapps.backend.domain.checklisttemplate.ChecklistTemplate;
import de.fivethree.immoapps.backend.domain.checklisttemplate.ChecklistTemplateLevel;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface ChecklistTemplateLevelRepository extends JpaRepository<ChecklistTemplateLevel, Long> {
    ChecklistTemplateLevel findByIdAndChecklistTemplate(Long id, ChecklistTemplate checklistTemplate);
}
