import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { KriteriumComponent } from './kriterium.component';

describe('KriteriumComponent', () => {
  let component: KriteriumComponent;
  let fixture: ComponentFixture<KriteriumComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ KriteriumComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(KriteriumComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
