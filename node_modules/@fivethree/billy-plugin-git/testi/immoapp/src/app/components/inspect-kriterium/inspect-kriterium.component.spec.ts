import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { InspectKriteriumComponent } from './inspect-kriterium.component';

describe('InspectKriteriumComponent', () => {
  let component: InspectKriteriumComponent;
  let fixture: ComponentFixture<InspectKriteriumComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ InspectKriteriumComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(InspectKriteriumComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
