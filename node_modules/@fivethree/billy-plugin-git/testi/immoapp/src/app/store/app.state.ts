import { AppContextStateModel, ContextState } from './context/context.state';
import { InspectionsState, InspectionsStateModel } from './inspections/inspections.state';
import { AuthState, AuthStateModel } from './auth/auth.state';
import { NotifyStateModel, NotifyState } from './notify/notify.state';
import { PropertiesState, PropertiesStateModel } from './properties/properties.state';
import { MessagesState, MessagesStateModel } from './messages/messages.state';
import { PushState, PushStateModel } from './push/push.state';

export interface AppState {
    auth: AuthStateModel;
    properties: PropertiesStateModel;
    inspections: InspectionsStateModel;
    messages: MessagesStateModel;
    notify: NotifyStateModel;
    push: PushStateModel;
    context: AppContextStateModel;
}

export const APP_STATES = [
    AuthState,
    PropertiesState,
    InspectionsState,
    MessagesState,
    NotifyState,
    PushState,
    ContextState
];
